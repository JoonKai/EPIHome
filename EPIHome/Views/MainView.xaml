<Window x:Class="EPIHome.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EPIHome.Views"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:combined="clr-namespace:EPIControls.Controls.CombinedUserControls;assembly=EPIControls"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="1400" Background="#FF313244">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0"
                        cal:View.Model="{Binding ActiveItem}"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"/>
        <Border Grid.Column="1" 
                Margin="8" 
                Background="#FF1E1E2E" 
                CornerRadius="6">
            <combined:EPIRndRobotViewer/>
        </Border>
    </Grid>
</Window>